# Frozen before the STSBENCHV1005 seed was observed. This is a single-seed
# capability scout, not a win-rate estimate.
schema_version = 1
experiment_id = "ascension-scout-v1"
strategy = "a20_then_a10_if_a20_loss"
frozen_at = "2026-08-16"

seed_set = "v1"
seed = "STSBENCHV1005"
character = "Ironclad"
ascensions = [20, 10]

backend = "codex-cli"
model = "gpt-5.6-sol"
reasoning_effort = "high"
temperature = 0.0
max_tokens = 768
history_turns = 2
model_timeout = 180.0

max_decisions = 1200
retry_budget = 2
max_attempts = 3
startup_timeout = 90.0
episode_timeout = 14400.0
restart_delay = 5.0
require_observer = true
caffeinate = true
resume = true

runs_root = "runs/ascension-scout-v1"
